[connection]

hostname = "greenmail" # TODO: change to "greenmail" when I make a new cert with greenmail SAN
username = "test@greenmail.com" # With or without the '@gmail.com'
password = "test@greenmail.com"
port = 3993

[imap_options]

sections = ["FLAGS", "INTERNALDATE", "RFC822", "ENVELOPE"]
